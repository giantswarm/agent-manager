apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-manager.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agent-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-manager.selectorLabels" . | nindent 4 }}
